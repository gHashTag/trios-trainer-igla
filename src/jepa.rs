//! T-JEPA façade. Migrated from `trios-train-cpu/src/jepa/`
//! (mod.rs, ema.rs, loss.rs, masking.rs, predictor.rs).
//!
//! Deduplicates the second JEPA path that lived in
//! `trios-igla-trainer/src/jepa_runner.rs` — that one is removed in the
//! consolidation PR.

pub fn placeholder() {}
